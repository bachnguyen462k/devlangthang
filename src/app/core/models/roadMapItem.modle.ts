export interface RoadMapItem {
    slug: string;
    title: string;
    description: string;
    tagList: string[];
    createdAt: string;
    imageUrl: string;
    category: string;
    view: string;
}